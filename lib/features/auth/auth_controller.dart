/// Simple authentication controller placeholder for the `auth` feature.
/// Replace with real implementation (notifier, repository, etc.).
class AuthController {
  /// Const constructor for placeholder.
  const AuthController();

  /// Returns whether the user is currently logged in.
  bool isLoggedIn() => false;
}
