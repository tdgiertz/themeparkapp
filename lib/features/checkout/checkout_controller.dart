/// Simple checkout controller placeholder for the `checkout` feature.
/// Replace with a Notifier or repository-backed implementation.
class CheckoutController {
  /// Const constructor for placeholder.
  const CheckoutController();

  /// Starts the checkout flow.
  void startCheckout() {}
}
